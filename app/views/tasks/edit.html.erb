<h1>タスクNo.<%= @task.id %>の編集</h1>
<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label 'task', :content %>
    <%= f.text_field :content %>
    <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧へ', tasks_path %>